using FluidSimulation;
using UnityEngine;

namespace FluidDemo
{
    /*
    public class FluidSimParticles
    {
        public int NumParticles { get; private set; }
        public FluidSimParticle[] Particles;
  
        public FluidSimParticles(int maxNumParticles)
        {
            NumParticles = 0;
            Particles = new FluidSimParticle[maxNumParticles];
            for (int i = 0; i < Particles.Length; i++)
            {
                Particles[i] = new FluidSimParticle
                {
                    Position = Vector2.zero,
                    Velocity = Vector2.zero,
                    SubstanceIndex = -1,
                    Id = -1,
                    Active = false
                };
            }
        }
    }
    */
}