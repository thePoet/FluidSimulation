using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Elemental
{
    public class LiquidSimulationSettings : MonoBehaviour
    {
       // public LiquidParticle.SimulationSettings settings;

        // Called if settings are changed in the inspector
        private void OnValidate()
        {
           // LiquidParticle.settings = settings;
        }
    }
}
